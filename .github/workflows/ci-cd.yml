name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.maheshbabu018 }}
          password: ${{ secrets.dckr_pat_ml3YYfrrDhir3OQLD50Fnhrp7AA }}

      - name: Build Docker image
        run: |
          docker build -t ${{ secrets.maheshbabu018 }}/python-app:latest .

      - name: Push Docker image
        run: |
          docker push ${{ secrets.maheshbabu018 }}/python-app:latest
